name: animated_charts
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobx_triple: ^1.1.3+1
  flutter_triple: ^1.2.7+4
  intl: ^0.18.0
  cupertino_icons: ^1.0.2
  dash_painter: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
